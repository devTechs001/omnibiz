{
  "name": "omnibiz",
  "version": "1.0.0",
  "description": "Comprehensive business management system",
  "main": "server/server.js",
  "homepage": "https://devtechs001.github.io/omnibiz",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server/server.js",
    "client": "cd client && npm run dev",
    "install:client": "cd client && npm install",
    "install:server": "cd server && npm install",
    "build": "cd client && npm install && npm run build",
    "build:gh-pages": "cd client && cross-env GITHUB_PAGES=true npm run build",
    "start": "cd server && node server.js",
    "postinstall": "cd server && npm install && npm run seed-admins",
    "predeploy": "npm run build:gh-pages",
    "deploy": "gh-pages -d client/dist -t true -u \"devTechs001 <devtechs842@gmail.com>\"",
    "electron": "electron .",
    "electron-dev": "concurrently \"npm run client\" \"wait-on http://localhost:5173 && electron .\"",
    "electron-build": "electron-builder"
  },
  "keywords": ["business", "management", "pos", "inventory"],
  "author": "ominbiz Team",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "concurrently": "^8.0.1",
    "gh-pages": "^5.0.0",
    "cross-env": "^7.0.3",
    "electron": "^25.0.0",
    "electron-builder": "^24.0.0",
    "wait-on": "^7.0.1"
  },
  "build": {
    "appId": "com.ominbiz.app",
    "productName": "OminBiz",
    "directories": {
      "buildResources": "build",
      "output": "dist-electron"
    },
    "files": [
      "client/dist/**/*",
      "server/**/*",
      "package.json"
    ],
    "win": {
      "target": ["nsis"],
      "icon": "build/icon.ico"
    },
    "mac": {
      "target": ["dmg"],
      "icon": "build/icon.icns"
    },
    "linux": {
      "target": ["AppImage"],
      "icon": "build/icon.png"
    }
  }
}
